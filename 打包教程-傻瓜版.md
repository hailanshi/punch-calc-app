# 打卡工资计算器 · 拿 .ipa 的傻瓜教程（不需要懂代码）

> 原理一句话：把文件传到 GitHub，GitHub 会免费借用苹果的电脑（Mac）帮你编译，
> 编译好自动生成一个 `punch-calculator.ipa`，你下载后装进巨魔商店（TrollStore）即可。

全程在手机或电脑的浏览器里点鼠标即可，不需要装任何软件、不需要输入命令。

---

## 第一步：注册 GitHub（只做一次，免费）
1. 打开 https://github.com/signup
2. 填邮箱、设密码、起个用户名，按提示验证。完成后登录。

## 第二步：建一个空仓库（存文件的地方）
1. 右上角 **+** → **New repository**（新建仓库）
2. Repository name 随便填，例如：`punch-calc`
3. 选 **Public**（公开即可，反正里面没有隐私数据）
4. 其他默认不动 → 点最下方绿色 **Create repository**（创建仓库）

## 第三步：把整个文件夹拖进去
1. 找到我给你的那个文件夹（解压后叫 `打卡工资计算器-打包源`）。
   确认里面能看到：`打卡工资计算器.html`、`ios-shell` 文件夹、`.github` 文件夹、`README.md`。
2. 回到刚建好的仓库页面 → 点 **Add file** → **Upload files**
3. 把 **`打卡工资计算器-打包源` 这整个文件夹** 用鼠标拖进网页的虚线框里
   （拖整个文件夹，浏览器会自动把里面所有文件——包括 `.github` 那个隐藏文件夹——都传上去）
4. 页面下方点绿色 **Commit changes**（提交更改）

## 第四步：等它自动编译（约 1～3 分钟）
1. 切到仓库顶部 **Actions** 标签
2. 你会看到一条叫 **Build PunchCalc IPA** 的记录，旁边是黄色圆圈（正在跑）
3. 等它变成绿色 ✓（成功）——红了也没关系，先告诉我就行

## 第五步：下载 .ipa
1. 点进那条绿色记录
2. 页面最下面 **Artifacts（成品）** → 下载 **punch-calculator-ipa**
3. 解压后得到一个 `punch-calculator.ipa`

## 第六步：装进 iPhone
1. 把 `punch-calculator.ipa` 弄到 iPhone（微信/QQ/网盘/隔空投送都行）
2. 用 **TrollStore（巨魔商店）** 打开这个文件 → 点 **Install**
3. 装好后桌面出现「打卡工资」图标，点开就能用。

---

## 如果你不想建 GitHub / 遇到任何一步不会
最简单的两条备用路：
- **问一个身边有 Mac（苹果电脑）的朋友**：把文件夹给他，双击打开 `ios-shell/PunchCalcApp`，在 Mac 上装个免费的 Xcode，5 分钟能编出来（需要帮忙就让他按本文件夹里 `ios-shell/README.md` 做）。
- **完全不要 .ipa 也能用**：`打卡工资计算器.html` 本身就能直接在 iPhone 浏览器里用（见根目录 `README.md`），只是要放进 Safari 需要先把它放到网上，功能一模一样。

卡在任何一步，把页面截图发给我，我一步步带你。
